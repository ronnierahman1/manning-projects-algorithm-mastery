# You will use NumPy to optimize grid and cost data handling

# Import NumPy
import numpy as np

def convert_grid_to_numpy(grid):
    """
    Convert a Python list of lists into a NumPy array for efficient access.

    Args:
        grid (list of list of int): Original grid.

    Returns:
        np.ndarray: NumPy version of the grid.
    """
    # Step 1: Convert grid to NumPy array
    # Write your code here
    pass

def generate_cost_map_numpy(grid):
    """
    Create a cost map (row, col) -> cost using NumPy operations.

    Args:
        grid (np.ndarray): Grid represented as a NumPy array.

    Returns:
        dict: Cost map dictionary using NumPy for speed.
    """
    # Step 1: Use grid.shape to get dimensions
    # Step 2: Create a nested loop or use vectorized indexing to assign variable costs
    # Step 3: Return a dictionary mapping (r, c) to cost

    # Write your code here
    pass
